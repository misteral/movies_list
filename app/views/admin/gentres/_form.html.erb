<%= form_with(model: gentre, local: true) do |form| %>
  <% if gentre.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(gentre.errors.count, "error") %> prohibited this gentre from being saved:</h2>

      <ul>
      <% gentre.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
